[project]
name = "omni-mcp-server"
version = "0.1.0"
description = "Asynchronous Model Context Protocol tools using native python typing"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "mcp>=1.0.0",
]
